# MongoDB Atlas Configuration
MONGODB_URL=__REPLACE_WITH_YOUR_MONGODB_URL__

# Security
SECRET_KEY=__REPLACE_WITH_A_STRONG_SECRET_KEY__
GEMINI_API_KEY=__REPLACE_WITH_YOUR_GEMINI_API_KEY__

# Production Settings
DEBUG=False
ENVIRONMENT=production

# CORS Origins (update with your frontend URL)
# Add both the old and new frontend URLs for compatibility
# Format: https://frontend-service-name.onrender.com
#CORS_ORIGINS=https://frontend-038v.onrender.com,https://frontend-c3bv.onrender.com

# Frontend URL (for activation links in emails)
#FRONTEND_URL=https://frontend-c3bv.onrender.com

# Other settings
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256
AUTH_ENABLED=True

# Security guidance:
# - Do NOT commit production secrets. Use Render environment variables (Dashboard → Environment)
# - To remove secrets from git history, use `git filter-repo` or contact support if needed.

